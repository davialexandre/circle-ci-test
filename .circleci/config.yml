# PHP CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
#
version: 2.1

jobs:
  build:
    docker:
      # Specify the version you desire here
      - image: circleci/php:7.2.19-cli-node

    steps:
      - checkout
      - run:
          command: |
            export PATH="$PATH:$HOME/.composer/vendor/bin"
            composer global require drupal/coder
            phpcs --config-set installed_paths ~/.composer/vendor/drupal/coder/coder_sniffer
            git diff-tree --no-commit-id --name-only -r HEAD | xargs phpcs --standard=Drupal --extensions=php,inc,module

#      - run:
#          name: "Run PHP Tests"
#          command: |
#            export PATH=/buildkit/buildkit/bin:$PATH
#            HAS_FAILED_TESTS=0
#            cd uk.co.compucorp.civicrm.hrcore
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/hrcore.xml || HAS_FAILED_TESTS=1
#            cd ../com.civicrm.hrjobroles
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/hrjobroles.xml || HAS_FAILED_TESTS=1
#            cd ../contactaccessrights
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/contactaccessrights.xml || HAS_FAILED_TESTS=1
#            cd ../uk.co.compucorp.civicrm.hrleaveandabsences
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/hrleaveandabsences.xml ||HAS_FAILED_TESTS=1
#            cd ../hrjobcontract
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/hrjobcontract.xml || HAS_FAILED_TESTS=1
#            cd ../uk.co.compucorp.civicrm.hrsampledata
#            phpunit4 --testsuite "Unit Tests" --log-junit /buildkit/project/test-results/phpunit/hrsampledata.xml || HAS_FAILED_TESTS=1
#
#            exit $HAS_FAILED_TESTS
#          working_directory: "/buildkit/buildkit/build/drupal-clean/sites/all/modules/civicrm/tools/extensions/civihr"

      # Download and cache dependencies
#      - restore_cache:
#          keys:
#            # "composer.lock" can be used if it is committed to the repo
#            - v1-dependencies-{{ checksum "composer.json" }}
#            # fallback to using the latest cache if no exact match is found
#            - v1-dependencies-
#
#      - run: composer install -n --prefer-dist
#
#      - save_cache:
#          key: v1-dependencies-{{ checksum "composer.json" }}
#          paths:
#            - ./vendor
#      - restore_cache:
#          keys:
#            - node-v1-{{ checksum "package.json" }}
#            - node-v1-
#      - run: yarn install
#      - save_cache:
#          key: node-v1-{{ checksum "package.json" }}
#          paths:
#            - node_modules

      # run tests with phpunit or codecept
#      - run: ./vendor/bin/phpunit --log-junit test-results/phpunit/results.xml
#      - run: ./vendor/bin/codecept build
#      - run: ./vendor/bin/codecept run

#      - store_test_results:
#          path: test-results
